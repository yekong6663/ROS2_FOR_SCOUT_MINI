# Used when launching navigation with SKIP_OUTBOUND=1.
# Record the chassis pose where skip-outbound actually starts
# (map frame, base_link). Do not reuse the map-origin initial_pose.yaml.
initial_pose:
  x: 0.0
  y: 0.0
  z: 0.0
  yaw: 0.0
  pitch: 0.0
  roll: 0.0
