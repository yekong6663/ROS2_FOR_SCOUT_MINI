image: nav2_map.png
mode: trinary
resolution: 0.05
origin: [-77.4, -32.7, 0]
negate: 0
occupied_thresh: 0.65
# Only the original near-white road pixels are free.  The gray pixels in the
# source image must remain unknown so the global planner (allow_unknown=false)
# never plans through them.
free_thresh: 0.01
