# Do not add this plugin to Nav2's planner_plugins list until createPlan()
# contains a complete A* implementation.
planner_server:
  ros__parameters:
    ScoutAstar:
      plugin: scout_navigation_plugins::ScoutAstarPlanner
